import React, { Component } from 'react';

class Playground extends Component {

    render() {
        return(
            <div id = {'playground'}>
                chat
            </div>
        )
    }
}

export default Playground