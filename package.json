{
  "name": "unogame",
  "version": "1.0.0",
  "main": "backend/bin/www.js",
  "scripts": {
    "start": "cd backend && npm run start",
    "heroku-postbuild": "cd frontend && npm install --only=dev && npm install && npm run build"
  }
}
